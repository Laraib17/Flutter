import 'package:flutter/material.dart';

class Container6Screen extends StatelessWidget {
  const Container6Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
